# Remove the "trigger moment" text on step 6

In `src/components/Day1Setup.tsx`, in the step 6 messages (around lines 1325–1333), delete the trailing `" The trigger moment."` phrase from both Johnny lines so the screen ends at the question mark:

- `... the perfect time to take your challenge? The trigger moment.` → `... the perfect time to take your challenge?`

No other changes. Step still exists, sequence and scoring untouched.
